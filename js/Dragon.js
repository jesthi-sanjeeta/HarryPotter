//this.body = creatSprite();
//display function not needed
// this class is for bad NPC

/*class Dragon{
    constructor(x, y, width, height) {
        var options = {
            isStatic: true,
        }
        this.body = Bodies.rectangle(x, y, width, height, options);
        this.width = width;
        this.height = height;
        World.add(world, this.body);
        this.image = loadImage("images/dragon.png");
        
        
      }
      display(){
        var pos1 = this.body.position;
        imageMode(CENTER);
        image(this.image,pos1.x,pos1.y,100,100);
      }
    }*/